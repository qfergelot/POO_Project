Version de base :

- affiner l'affichage de la map pour qu'elle soit correcte sur tous les écrans

- si on clique sur le bouton charger sur le menu et qu'il n'y a pas de fichier de sauvegarde, ça lance quand meme une partie comme si on cliquait sur jouer.

- ne pas oublier dans le readme, touche L in game = Load


-javadoc :
	game:
		Input (to test) en
		Main (to test) en
		Popup (to test) en
		Sprite (to test) en
		UISingleton (to test) en
	royaume:
		Castle (to test) en
		Constants (to test) en
		Duke (to test) en
		IABasic (to test) en
		Order (doing) en
		Ost (to test) en
		Door (to test) en
		ProdException (to test) en
		Production (to test) en
		Kingdom (to test) en
	troupes:
		Knight (to test) en
		Onager (to test) en
		Pikeman (to test) en
		Troop (to test) en
		
-relire la doc pour corriger

Version Avancée (A FAIRE QUE QUAND ON AURA RENDU LA BASIQUE) :

- mettre un message "en pause" quand le jeu est en pause

- IA Avancée

-si on commande une production alors qu'il y en a une en cours, ajouter la production dans une file 

-peut-être une sauvegarde plus élaborée ?

-javadoc de tous ce qui est nouveau ;)